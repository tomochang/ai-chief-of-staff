{
  "name": "ai-chief-of-staff",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "test": "npm run test:js && npm run test:bash && npm run test:e2e",
    "test:js": "vitest run",
    "test:bash": "bats tests/bash/*.bats",
    "test:e2e": "bash scripts/e2e-smoke.sh",
    "eval:e2": "node scripts/evaluate-triage.js --file examples/metrics/triage-labeled.sample.jsonl",
    "setup:hooks": "bash scripts/setup-git-hooks.sh",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
