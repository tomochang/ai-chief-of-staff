{
  "name": "ai-chief-of-staff",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "test": "npm run test:js && npm run test:bash",
    "test:js": "vitest run",
    "test:bash": "bats tests/bash/*.bats",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
